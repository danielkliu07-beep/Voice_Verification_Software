[project]
name = "voice-verification-software"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "danielkliu07-beep", email = "danielkliu07@gmail.com" }
]
requires-python = ">=3.13.0"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "speechbrain>=1.1.0",
]

[project.scripts]
voice-verification-software = "voice_verification_software:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
